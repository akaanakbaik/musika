{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "build": "bun x tsc --build && bun --cwd artifacts/api-server run build",
    "typecheck": "bun x tsc --build"
  },
  "private": true,
  "packageManager": "bun@1.2.8",
  "devDependencies": {
    "prettier": "^3.8.1",
    "typescript": "~5.9.2"
  },
  "dependencies": {
    "@types/nodemailer": "^8.0.0",
    "@types/pg": "^8.18.0",
    "nodemailer": "^8.0.6",
    "pg": "^8.20.0"
  }
}
