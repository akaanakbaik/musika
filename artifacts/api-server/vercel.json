{
  "buildCommand": "bun ./build.mjs",
  "outputDirectory": "dist",
  "installCommand": "npm install -g bun pnpm && pnpm install --no-frozen-lockfile --ignore-scripts",
  "framework": "node",
  "functions": {
    "index.mjs": {
      "maxDuration": 30
    }
  }
}
